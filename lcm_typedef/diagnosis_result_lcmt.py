"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""


from io import BytesIO
import struct

class diagnosis_result_lcmt(object):

    __slots__ = ["timestamp", "component", "status", "detail"]

    __typenames__ = ["int64_t", "string", "int32_t", "string"]

    __dimensions__ = [None, None, None, None]

    def __init__(self):
        self.timestamp = 0
        """ LCM Type: int64_t """
        self.component = ""
        """ LCM Type: string """
        self.status = 0
        """ LCM Type: int32_t """
        self.detail = ""
        """ LCM Type: string """

    def encode(self):
        buf = BytesIO()
        buf.write(diagnosis_result_lcmt._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">q", self.timestamp))
        __component_encoded = self.component.encode('utf-8')
        buf.write(struct.pack('>I', len(__component_encoded)+1))
        buf.write(__component_encoded)
        buf.write(b"\0")
        buf.write(struct.pack(">i", self.status))
        __detail_encoded = self.detail.encode('utf-8')
        buf.write(struct.pack('>I', len(__detail_encoded)+1))
        buf.write(__detail_encoded)
        buf.write(b"\0")

    @staticmethod
    def decode(data: bytes):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != diagnosis_result_lcmt._get_packed_fingerprint():
            raise ValueError("Decode error")
        return diagnosis_result_lcmt._decode_one(buf)

    @staticmethod
    def _decode_one(buf):
        self = diagnosis_result_lcmt()
        self.timestamp = struct.unpack(">q", buf.read(8))[0]
        __component_len = struct.unpack('>I', buf.read(4))[0]
        self.component = buf.read(__component_len)[:-1].decode('utf-8', 'replace')
        self.status = struct.unpack(">i", buf.read(4))[0]
        __detail_len = struct.unpack('>I', buf.read(4))[0]
        self.detail = buf.read(__detail_len)[:-1].decode('utf-8', 'replace')
        return self

    @staticmethod
    def _get_hash_recursive(parents):
        if diagnosis_result_lcmt in parents: return 0
        tmphash = (0xd949e19017644042) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff) + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _packed_fingerprint = None

    @staticmethod
    def _get_packed_fingerprint():
        if diagnosis_result_lcmt._packed_fingerprint is None:
            diagnosis_result_lcmt._packed_fingerprint = struct.pack(">Q", diagnosis_result_lcmt._get_hash_recursive([]))
        return diagnosis_result_lcmt._packed_fingerprint

    def get_hash(self):
        """Get the LCM hash of the struct"""
        return struct.unpack(">Q", diagnosis_result_lcmt._get_packed_fingerprint())[0]

